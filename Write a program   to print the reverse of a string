#include <stdio.h>
#include <string.h>

int main() {
    char str[100], reversed[100];
    int length, i, j = 0;

    // Ask user for input
    printf("Enter a string: ");
    fgets(str, sizeof(str), stdin); // Using fgets to read a string with spaces

    // Remove the newline character from fgets if present
    str[strcspn(str, "\n")] = '\0';

    // Calculate the length of the string
    length = strlen(str);

    // Reverse the
